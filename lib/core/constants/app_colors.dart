import 'package:flutter/material.dart';

/// Shared app color tokens.
abstract final class AppColors {
  /// Primary text and brand color.
  static const primary = Color(0xFF0F172A);

  /// Accent and interactive color.
  static const accent = Color(0xFF2563EB);

  /// App background color.
  static const background = Color(0xFFF8FAFC);

  /// Card and navigation surface color.
  static const surface = Color(0xFFFFFFFF);

  /// Muted input and chip surface color.
  static const surfaceVariant = Color(0xFFF1F5F9);

  /// Secondary text color.
  static const secondaryText = Color(0xFF64748B);

  /// Subtle border color.
  static const borderSubtle = Color(0xFFE2E8F0);

  /// Success text color.
  static const successText = Color(0xFF16A34A);

  /// Success background color.
  static const successBackground = Color(0xFFDCFCE7);

  /// Error text color.
  static const errorText = Color(0xFFDC2626);

  /// Error background color.
  static const errorBackground = Color(0xFFFEE2E2);

  /// Subtle card shadow color.
  static const cardShadow = Color(0x14000000);
}
